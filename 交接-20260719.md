# 今日改动交接（2026-07-19）

**起点** `034e6e6` → **当前** `f66cf0a`，共 5 个提交，改了 7 个文件。全部已 push 到 main 并部署上线。
OpenCode 先 `git pull` 同步（本地可能落后线上）。

---

## 1. `22814e7` — 修低消开关被批量改价冲掉

- **问题**：客户更新接口那行低消字段是无条件写入，批量改价没带这个字段时，会把已关的低消刷回"开启"。
- **改了**：`src/app/api/customers/route.ts` —— 改成"请求没带 enableMinVolume 就不动它"（跟其他字段一致）。
- **注意**：线上客户的低消数据是用户自己去后台重关的，代码只保证不再被冲掉。

## 2. `c9b9108` — 装柜批次详情三处修复

- `src/app/(main)/loading-lists/[id]/page.tsx`：明细表按**运单号**做二级合并单元格（运单号/仓库/总体积/货型/运输/单价/应收合并），不再每条重复看着乱。
- `src/app/(main)/loading-lists/[id]/page.tsx`：**总立方**原来把每条重复的"运单总体积"全加导致虚高 → 按运单号去重、每运单只算一次。
- `src/app/api/loading-items/route.ts`：**导入时后端直接算客户应收**落库（客户价 × max(运单总体积, 低消 海0.5/陆0.3)，落运单首条其余0，口径与生成账单一致），不用再先跑 AI 分类才有应收。

## 3. `4966436` — 修应收合计显示为空（¥—）

- **根因**：线上 `loading_items.客户应收` 列是 **TEXT 类型**（表结构漂移，代码定义是 real），写进去的数字被存成字符串。JS 里 `0 + "1707.59"` 变字符串拼接 → 求和结果非数字 → 显示 `¥—`。
- **改了**：所有对客户应收做**加法求和**的地方加 `Number()`：
  - `src/app/(main)/loading-lists/[id]/page.tsx`
  - `src/app/(main)/page.tsx`（仪表盘本月营收 4 处）
  - `src/app/(main)/marks/[id]/page.tsx`
- **遗留待办**：线上这列仍是 TEXT 亲和性，数据 CAST 回 REAL 也会被转回字符串，现靠 `Number()` 兜底。**要根治得重建这列为 REAL**（生产表结构迁移，暂没做）。

## 4. `38fef03` — 装柜明细表加边框（非本次 AI 改动，用户/linter 所改）

- `src/app/(main)/loading-lists/[id]/page.tsx`：表格加清晰边框 + 外层 `overflow-auto` 滚动容器。

## 5. `f66cf0a` — 金额 2 位小数 + 本柜成本小结重构

- `src/lib/format.ts`：`formatAmount` 全站金额 **6 位 → 2 位**四舍五入。装柜单价列、拼柜货款显示同步改 2 位。**体积/立方保留 6 位精度**（不是价格）。
- `src/app/(main)/loading-lists/[id]/page.tsx`：本柜成本小结拆成 **货物成本(货款=需支付总价之和) + 各项费用(费用管理) + 总成本(货款+费用) + 应收合计**，去掉重复的"总费用""明细应收"卡片。
- `src/app/(main)/shared-containers/[id]/ClassifyButton.tsx`：拼柜货款显示 2 位。

---

## 涉及文件汇总

```
src/app/(main)/loading-lists/[id]/page.tsx          （合并显示/总立方/应收求和/成本小结/单价2位）
src/app/(main)/marks/[id]/page.tsx                  （应收求和转数字）
src/app/(main)/page.tsx                             （仪表盘营收求和转数字）
src/app/(main)/shared-containers/[id]/ClassifyButton.tsx （货款2位）
src/app/api/customers/route.ts                      （低消开关不覆盖）
src/app/api/loading-items/route.ts                  （导入时算客户应收）
src/lib/format.ts                                   （金额2位小数）
```

## 部署流程（标准）

```
本地改 → npx tsc --noEmit → git commit + git push（main）
服务器：git pull + npm run build + pm2 restart finance-system
```
（改 table_parser.py 才需额外 pm2 restart table-parser；本次没动）

## 两个遗留待办

1. 线上 `loading_items.客户应收` 列 **TEXT → REAL** 重建（根治类型漂移，现靠 Number() 兜底）。
2. 账单 **Excel 导出**的小数位还没统一成 2 位（本次只改了页面显示）。
